import styled from "@emotion/styled";

import { spacing } from '../../styles/variables/_variables'
import {rem} from "polished";

const LandingHeroStyles = styled.div`
  padding: ${ rem(spacing.xl) } 0;
`;

export default LandingHeroStyles;